# Proposal

Most ECG repositories ask which model gets the highest accuracy. This repository asks which model remains reliable when signals become noisy, incomplete, or shifted.

The contribution is reliability, calibration, and robustness rather than yet another neural network.